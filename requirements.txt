# StardustFS 의존성 (pinned versions)

# 암호화
cryptography==48.0.0

# 메타데이터 DB 암호화 (Linux 전용, Windows에서는 표준 sqlite3 폴백)
# pysqlcipher3==1.2.1

# 비동기 HTTP 클라이언트
httpx==0.28.1

# .env 파일 로드 (개발/디버그용)
python-dotenv==1.1.0

# P2P 서버 (aiohttp)
aiohttp==3.11.18

# NAT 트래버설 (Pure Python UPnP — C 컴파일러 불필요)
async-upnp-client==0.42.0

# GUI 시스템 트레이 (선택 — 미설치 시 트레이 없이 창닫기=종료로 동작)
# 둘 다 prebuilt wheel 제공(C 컴파일러 불필요)
pystray==0.19.5
Pillow==10.4.0

# 테스트
hypothesis==6.152.9
pytest==8.3.4
pytest-asyncio==0.26.0
pytest-httpx==0.35.0
pytest-aiohttp==1.0.5
requests==2.34.2
