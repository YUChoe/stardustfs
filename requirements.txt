# StardustFS 의존성 (pinned versions)

# 암호화
cryptography==48.0.0

# 메타데이터 DB 암호화 (Linux 전용, Windows에서는 표준 sqlite3 폴백)
# pysqlcipher3==1.2.1

# 비동기 HTTP 클라이언트
httpx==0.28.1

# .env 파일 로드 (개발/디버그용)
python-dotenv==1.1.0

# P2P 서버 (aiohttp)
aiohttp==3.11.18

# GUI 시스템 트레이 (선택 — 미설치 시 트레이 없이 창닫기=종료로 동작)
# 둘 다 prebuilt wheel 제공(C 컴파일러 불필요)
pystray==0.19.5
Pillow==10.4.0

# GUI 현대적 테마(Windows 11 풍 플랫). 미설치 시 기본 ttk로 폴백(pure-python)
sv-ttk==2.6.1

# 루프백 스토리지: 파일 내 FAT 파일시스템(순수 파이썬, 권한 불필요).
# LoopbackSource가 고정 크기 FAT 이미지 안에 파일을 저장한다.
pyfatfs==1.1.0
fs==2.4.16
appdirs==1.4.4

# 테스트
hypothesis==6.152.9
pytest==8.3.4
pytest-asyncio==0.26.0
pytest-httpx==0.35.0
pytest-aiohttp==1.0.5
